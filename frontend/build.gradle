plugins {
    id 'org.siouan.frontend-jdk11'
}

frontend {
    nodeVersion = '16.14.2'
    assembleScript = 'run build --max-old-space-size=200'
    cleanScript = 'run clean'
    checkScript = 'run check'
}